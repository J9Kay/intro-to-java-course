package com.cbfacademy;

import java.nio.file.Files;
import java.util.ArrayList;
import java.util.List;
import static java.nio.file.StandardCopyOption.*;

public class FileManager {
    String filename;
    String inputFile;
    String outputFile;
 
    
    public List<String> readFile(String filename) {
        List<String> file_list = new ArrayList<>();
        
        return file_list;
    }
    // append reading of each line and append to the array.
    //    private void copyFileUsingStream(String inputFile2, String outputFile2) {
    // }

    public void copy(String inputFile, String outputFile) {
        copy(inputFile, outputFile);


    }

//  read the file and write the file to the output file
}
